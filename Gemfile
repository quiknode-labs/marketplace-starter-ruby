# frozen_string_literal: true

source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

ruby '3.1.2'

# server
gem 'sinatra'

# database
gem 'pg'
gem 'sequel'

# prod server
gem 'puma'

# other
gem 'redis'
gem 'json'
gem 'jwt'
gem 'dotenv'
gem 'httparty'
gem 'rake'